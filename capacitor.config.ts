import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Shebby Meal Prepper',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
